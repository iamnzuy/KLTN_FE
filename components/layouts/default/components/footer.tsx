'use client';

import { GENERAL_SETTINGS } from '@/config/app.config';

export function Footer() {
  const currentYear = new Date().getFullYear();

  return (
    <footer className="footer">
      <div className="container">
      </div>
    </footer>
  );
}
